// 1. 作ったテンプレートを読み込む
#import "theme.typ": *

// 2. 設定を適用する
#show: presentation.with(
  title: "絶滅危惧種保護法の評価",
  authors: "今村隼人",
  // 必要なら subtitle: "...", date: "..." も追加可能
)

// === ここから中身を書くだけ！ ===

== ESAが開発を促進してしまう矛盾

#center-slide[ // 用意したショートカットで上下中央揃え
  - 絶滅危惧種の多くは *私有地* に生息している
  - 開発規制はあるが、土地所有者への *十分な補償がない*
  - 結果、所有地に「逆のインセンティブ」が働く
    - 「規制される前に開発してしまおう」という *駆け込み開発* が発生
    - (Preemptive habitat destruction)
]

#pagebreak()

== 分析方法：Ferraro et al. (2007)

#center-slide[
  - データと結果変数 $Y_i$
    - アメリカ国内の絶滅の恐れのある生物 $i$ の *絶滅危険度指数*

  - トリートメント変数 $D_i$ と比較対象
    + リストへの *指定のみ*
    + 指定 + *高額* 助成金
    + 指定 + *少額* 助成金
    - *コントロール群：ESA未指定*
]

#pagebreak()

== 推定結果と考察

// ...（表などはここに書く）...

#v(1em)

// 用意したショートカットで強調ボックスを作る
#focus-box[
  *考察:* リストの拡大（指定）だけでは不十分。
  開発規制に対する補償など、 *十分な助成金の拠出* こそが重要である。
]